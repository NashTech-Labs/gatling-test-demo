gatling {
  baseUrl {
    gatlingMeetup = "http://localhost:8080"
  }
  usersAtOnce = 15
  usersAtOnce = ${?USERS_AT_ONCE}
  rampUsers = 25
  rampUsers = ${?RAMP_USERS}
  rampOverTime = 45
  rampOverTime = ${?RAMP_OVER_TIME}
  feederPath = "emailFeeder.csv"
  responseTime = 50
  responseTime = ${?RESPONSE_TIME}
  failedRequests = 5
  failedRequests = ${?FAILED_REQUESTS}
}